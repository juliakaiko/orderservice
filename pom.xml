<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.4</version>
		<relativePath/>
	</parent>
	<groupId>com.mymicroservice</groupId>
	<artifactId>orderservice</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>orderservice</name>
	<description>Demo project for Spring Boot</description>
	<properties>
		<java.version>21</java.version>
		<org.projectlombok.version>1.18.32</org.projectlombok.version>
		<liquibase-maven-plugin.version>4.27.0</liquibase-maven-plugin.version>
		<org.liquibase.version>4.27.0</org.liquibase.version>
		<springdoc-openapi.version>2.5.0</springdoc-openapi.version>
		<mapstruct.version>1.6.0.Beta2</mapstruct.version>
		<lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
		<org.testcontainers.version>1.19.8</org.testcontainers.version>
		<jjwt.version>0.11.5</jjwt.version>
		<sonar.organization>juliakaiko</sonar.organization>
		<sonar.projectKey>juliakaiko_orderservice</sonar.projectKey>
		<sonar.host.url>https://sonarcloud.io</sonar.host.url>
		<sonar.java.binaries>target/classes</sonar.java.binaries>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring-cloud-starter-openfeign.version>4.1.5</spring-cloud-starter-openfeign.version>
        <logstash-logback-encoder.version>7.4</logstash-logback-encoder.version>
        <jackson-databind.version>2.17.0</jackson-databind.version>
        <spring-cloud-contract-wiremock.version>4.1.1</spring-cloud-contract-wiremock.version>
        <wiremock-jre8.version>2.35.0</wiremock-jre8.version>
        <spring-security-test.version>6.5.2</spring-security-test.version>
        <common-events.version>1.0.0</common-events.version>
        <postgresql.version>42.7.4</postgresql.version>
        <common-filters-starter.version>1.0.1</common-filters-starter.version>
        <uuid-creator.version>5.3.0</uuid-creator.version>
        <shedlock-provider-jdbc-template.version>4.45.0</shedlock-provider-jdbc-template.version>
        <shedlock-spring.version>4.45.0</shedlock-spring.version>
        <wiremock.version>3.5.4</wiremock.version>
        <openapi-generator.version>7.10.0</openapi-generator.version>
	</properties>
	<repositories>
        <repository>
			<id>github-event</id>
			<name>GitHub Packages</name>
			<url>https://maven.pkg.github.com/juliakaiko/common-events</url>
		</repository>
        <repository>
            <id>github-filter</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/juliakaiko/common-filters-spring-boot-starter</url>
        </repository>
	</repositories>

	<dependencies>
        <dependency>
            <groupId>com.mymicroservice</groupId>
            <artifactId>common-filters-starter</artifactId>
            <version>${common-filters-starter.version}</version>
        </dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- For cron job (partitioning) -->
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-spring</artifactId>
            <version>${shedlock-spring.version}</version>
        </dependency>
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-provider-jdbc-template</artifactId>
            <version>${shedlock-provider-jdbc-template.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.f4b6a3</groupId>
            <artifactId>uuid-creator</artifactId>
            <version>${uuid-creator.version}</version>
        </dependency>

        <!-- Spring Security + JWT (OAuth2 Resource Server) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>

		<!-- Feign Client -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-openfeign</artifactId>
			<version>${spring-cloud-starter-openfeign.version}</version>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
		</dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${org.projectlombok.version}</version>
            <scope>provided</scope>
        </dependency>

		<!-- Mapstruct for creating DTO-->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>

		<!-- Kafka -->
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka</artifactId>
		</dependency>

		<!-- Own library for DTO in Kafka -->
		<dependency>
			<groupId>com.mymicroservices</groupId>
			<artifactId>common-events</artifactId>
			<version>${common-events.version}</version>
		</dependency>

		<!--  Swagger -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc-openapi.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson-databind.version}</version>
		</dependency>

        <!-- For encoding logs in JSON -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>${logstash-logback-encoder.version}</version>
        </dependency>

		<!-- Testing -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- For integration Feign and WireMock -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-contract-wiremock</artifactId>
            <version>${spring-cloud-contract-wiremock.version}</version>
			<scope>test</scope>
		</dependency>

        <!-- WireMock for mocking HTTP requests -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock</artifactId>
            <version>${wiremock.version}</version>
            <scope>test</scope>
        </dependency>

		<!-- Testcontainers -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<version>${org.testcontainers.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${org.testcontainers.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<version>${org.testcontainers.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>kafka</artifactId>
			<version>${org.testcontainers.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- JWT -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<version>${spring-security-test.version}</version>
			<scope>test</scope>
		</dependency>
    </dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${org.testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
            <!-- OpenAPI Generator: генерирует Spring-интерфейсы API из api.yaml на этапе generate-sources -->
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${openapi-generator.version}</version>
                <executions>
                    <execution>
                        <!-- id — уникальный идентификатор execution внутри pom.xml -->
                        <id>orders-api</id>
                        <goals>
                            <!-- goal generate — запускает генерацию Java-классов из OpenAPI-спецификации -->
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <!-- inputSpec — путь к OpenAPI-файлу (контракт API) -->
                            <inputSpec>${project.basedir}/api.yaml</inputSpec>
                            <!-- modelPackage — пакет для DTO-моделей (не генерируем, используем существующие) -->
                            <modelPackage>com.mymicroservice.orderservice.api.model</modelPackage>
                            <!-- generateModels=false — не создавать DTO, чтобы избежать дубликатов с dto-пакетом -->
                            <generateModels>false</generateModels>
                            <!-- apiPackage — пакет для сгенерированных API-интерфейсов -->
                            <apiPackage>com.mymicroservice.orderservice.api</apiPackage>
                            <!-- generatorName=spring — генератор для Spring Boot REST API -->
                            <generatorName>spring</generatorName>
                            <!-- library=spring-boot — использовать Spring Boot аннотации и стиль -->
                            <library>spring-boot</library>
                            <configOptions>
                                <!-- interfaceOnly=true — генерировать только интерфейс, реализация пишется вручную -->
                                <interfaceOnly>true</interfaceOnly>
                                <!-- useTags=true — группировать операции по OpenAPI-тегам (Orders → OrdersApi) -->
                                <useTags>true</useTags>
                                <!-- useSpringBoot3=true — совместимость с Spring Boot 3 / Jakarta EE -->
                                <useSpringBoot3>true</useSpringBoot3>
                                <!-- useBeanValidation=true — добавить @Valid/@NotNull из схемы OpenAPI -->
                                <useBeanValidation>true</useBeanValidation>
                                <!-- skipDefaultInterface=true — не создавать default-методы в интерфейсе -->
                                <skipDefaultInterface>true</skipDefaultInterface>
                                <!-- containerDefaultToNull=true — коллекции по умолчанию null вместо пустых списков -->
                                <containerDefaultToNull>true</containerDefaultToNull>
                            </configOptions>
                            <!-- typeMappings — замена типов OpenAPI на Java-типы -->
                            <typeMappings>
                                <!-- OffsetDateTime=Instant — маппинг даты/времени на java.time.Instant -->
                                <typeMapping>OffsetDateTime=Instant</typeMapping>
                                <typeMapping>PageOrderDto=Page</typeMapping>
                            </typeMappings>
                            <!-- importMappings — явные import для замapped-типов -->
                            <importMappings>
                                <importMapping>OffsetDateTime=java.time.Instant</importMapping>
                                <importMapping>OrderDto=com.mymicroservice.orderservice.dto.OrderDto</importMapping>
                                <importMapping>OrderItemDto=com.mymicroservice.orderservice.dto.OrderItemDto</importMapping>
                                <importMapping>OrderWithUserResponse=com.mymicroservice.orderservice.dto.OrderWithUserResponse</importMapping>
                                <importMapping>UserDto=com.mymicroservice.orderservice.dto.UserDto</importMapping>
                                <importMapping>OrderStatus=com.mymicroservice.orderservice.model.enums.OrderStatus</importMapping>
                                <importMapping>PageOrderDto=org.springframework.data.domain.Page</importMapping>
                                <importMapping>Page=org.springframework.data.domain.Page</importMapping>
                            </importMappings>
                            <!-- additionalProperties — дополнительные настройки генератора -->
                            <additionalProperties>
                                <!-- removeEnumValuePrefix=false — не удалять префиксы у значений enum -->
                                <removeEnumValuePrefix>false</removeEnumValuePrefix>
                            </additionalProperties>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- build-helper: добавляет сгенерированные исходники в classpath компиляции -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>add-generated-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/openapi/src/main/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${project.parent.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.liquibase</groupId>
				<artifactId>liquibase-maven-plugin</artifactId>
				<version>${org.liquibase.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.8.1</version>
				<configuration>
					<source>21</source>
					<target>21</target>
					<fork>true</fork>  <!-- compilation must be performed in a separate JVM process! -->
					<annotationProcessorPaths>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${mapstruct.version}</version>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>${lombok-mapstruct-binding.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<!--For correct testing-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
                    <systemPropertyVariables>
                        <spring.profiles.active>test</spring.profiles.active>
                        <testcontainers.dockerclient.version>1.44</testcontainers.dockerclient.version>
                    </systemPropertyVariables>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                    <forkCount>1</forkCount>
                    <reuseForks>false</reuseForks>
                    <parallel>none</parallel>
                    <runOrder>filesystem</runOrder>
                    <forkedProcessTimeoutInSeconds>300</forkedProcessTimeoutInSeconds>
				</configuration>
			</plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <!-- Attaches the Java Agent before running tests -->
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <!-- Generates an XML report after test execution -->
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
		</plugins>
	</build>
</project>
